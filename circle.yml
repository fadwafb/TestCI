#
# Build configuration for Circle CI
#

general:
    artifacts:
        - C:\Users\Fadwa\Desktop\TestCI\app\build\outputs\apk

machine:
    environment:
        ANDROID_HOME: D:\adt-bundle-windows-x86_64-20140702\sdk

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-23.0.1,android-23,extra-google-m2repository,extra-google-google_play_services,extra-android-support
        - ANDROID_HOME = D:\adt-bundle-windows-x86_64-20140702\sdk ./gradlew dependencies

test:
    override:
        - (./gradlew assemble):
            timeout: 360